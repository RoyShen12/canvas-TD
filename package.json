{
  "scripts": {
    "serve": "live-server --port=5000 --host=localhost --no-css-inject --ignore=**/src/**,**/.git/**",
    "dev": "tsc -w",
    "start": "concurrently \"pnpm:serve\" \"pnpm:dev\"",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.171",
    "@types/mainloop.js": "^1.0.5",
    "concurrently": "^6.2.0",
    "live-server": "^1.2.1",
    "prettier": "^2.3.2",
    "prettylint": "^1.0.0",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "typescript": "^4.3.5"
  }
}
